syntax = "proto3";

package module_service;

service ModuleService {
    rpc test_call(TestRequest) returns (TestResponse);
}
message TestRequest {
    bytes input_tensor_bytes = 1;
}

message TestResponse {
    bytes output_tensor_bytes = 1;
}